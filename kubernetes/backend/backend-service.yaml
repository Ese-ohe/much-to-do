apiVersion: v1
kind: Service
metadata:
  name: muchtodo-backend
  namespace: muchtodo
spec:
  type: NodePort
  selector:
    app: muchtodo-backend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30007
